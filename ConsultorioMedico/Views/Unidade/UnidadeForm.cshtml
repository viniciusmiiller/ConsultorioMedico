@model ConsultorioMedico.ViewModels.UnidadeFormViewModel
@{
    ViewBag.Title = Model.Title;
}

<div class="row">
    <div class="col col-md-6 col-md-offset-3">
        <h2>@ViewBag.Title</h2>

        @using (Html.BeginForm("Save", "Unidade"))
        {
            @Html.ValidationSummary(true, "Please fix the following errors.")
            <div class="form-group">
                @Html.LabelFor(m => m.Unidade.Nome)
                @Html.TextBoxFor(m => m.Unidade.Nome, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Unidade.Nome)
            </div>

            @Html.HiddenFor(m => m.Unidade.Id)
            <button type="submit" class="btn btn-sm btn-primary">Salvar</button>
        }
    </div>
</div>

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
